#include <iostream> //This is cld library of code  
using namespace std; // use the namespace std to avoid std :: before every cout

int main() // the execution of program starts from here and main part of programming
{
    float a = 3;    // 'a' can now store decimal numbers and a is the name of varible
    cout << a;         // prints 3.14 b/c float (the float data type usrd to print decimal number in the function )

    return 0;       // program finished successfully   
}
